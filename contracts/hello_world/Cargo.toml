[package]
name = "hello_world"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev_dependencies]
soroban-env-id = { workspace = true }
stellar-xdr = { workspace = true, features = ["wrap"] }